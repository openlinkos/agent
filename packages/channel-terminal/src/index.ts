/**
 * @openlinkos/channel-terminal — Terminal/stdin channel adapter.
 *
 * Provides a readline-based channel with ANSI colored output and
 * streaming token display for interactive terminal sessions.
 */

export { TerminalChannel } from "./terminal.js";
export type { TerminalChannelConfig } from "./terminal.js";
